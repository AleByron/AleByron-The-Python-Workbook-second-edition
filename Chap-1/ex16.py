import math
radius = float(input('Insert the radius value:' ))

area = math.pi*(radius**2)
volume = 4/3*math.pi*(radius**3)

print('The area of your sphere is:',area,',while the volume of your sphere is',volume)
